apiVersion: v1
kind: Service
metadata:
  name: model-server
  namespace: mlops-platform
spec:
  type: ClusterIP
  selector:
    app: model-server
  ports:
    - name: http
      port: 80
      targetPort: http
